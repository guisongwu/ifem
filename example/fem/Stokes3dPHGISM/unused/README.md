# Archived Stokes3d Scripts

This directory keeps earlier rectangular and ISMIP-HOM-C forward examples
that were superseded by the generic `NonlinearStokes3P2P1.m` solver and the
standalone ISMIP-HOM scripts in the parent directory.
